[tool.poetry]
name = "aegivion-shared-types"
version = "0.1.0"
description = "Shared Python model types and schemas for Aegivion"
authors = ["Aegivion Team <support@aegivion.com>"]

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^2.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
